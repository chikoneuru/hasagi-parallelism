global:
  scrape_interval: 15s

scrape_configs:
  - job_name: hasagi-orchestrator
    static_configs:
      - targets: ["orchestrator:8000"]
    metrics_path: /metrics

  - job_name: hasagi-workers
    dns_sd_configs:
      - names: ["worker-1", "worker-2"]
        type: A
        port: 9001
