apiVersion: apps/v1
kind: Deployment
metadata:
  name: tare-redis
  namespace: tare
spec:
  replicas: 1
  selector:
    matchLabels: {app: tare-redis}
  template:
    metadata:
      labels: {app: tare-redis}
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports: [{containerPort: 6379}]
---
apiVersion: v1
kind: Service
metadata:
  name: tare-redis
  namespace: tare
spec:
  selector: {app: tare-redis}
  ports:
    - {port: 6379, targetPort: 6379}
