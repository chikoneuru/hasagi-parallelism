# Copy this file to `.env` (which is gitignored) and paste your token:
#   cp .env.example .env && $EDITOR .env
#
# A free ElectricityMaps account gives a token; historical (>24h) past-range
# access needs the academic programme: https://www.electricitymaps.com/research
# The fetcher reads this var (experiments/fetch_electricitymaps_traces.py).
ELECTRICITYMAPS_TOKEN=
