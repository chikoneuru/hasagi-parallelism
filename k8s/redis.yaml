apiVersion: apps/v1
kind: Deployment
metadata:
  name: hasagi-redis
  namespace: hasagi
spec:
  replicas: 1
  selector:
    matchLabels: {app: hasagi-redis}
  template:
    metadata:
      labels: {app: hasagi-redis}
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports: [{containerPort: 6379}]
---
apiVersion: v1
kind: Service
metadata:
  name: hasagi-redis
  namespace: hasagi
spec:
  selector: {app: hasagi-redis}
  ports:
    - {port: 6379, targetPort: 6379}
